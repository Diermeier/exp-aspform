@model CustomerViewModel

@using (@Html.BeginForm())
{
    @Html.EditorFor(m => m, new { htmlAttributes = new { @class = "form-control" } });
    
    <input type="submit" value="Save" name="edit" id="btnSubmit" />
}